{
  "name": "animal-rescue-feedback",
  "version": "1.0.0",
  "description": "Animal rescue volunteer feedback system",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}